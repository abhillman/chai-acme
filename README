Install nix (https://nixos.org/download) and enter the virtual environment with a bash shell using `SHELL_ONLY=t ./python311`.

From within the virtual environment:
* run the demo with `python3 -m main '<search-term>'`
* run tests with `./tools/test`
