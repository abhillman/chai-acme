Install nix (https://nixos.org/download) then run `./python311 gscholar.py <query-text>`

In order to enter the virtual environment with a bash shell, run `SHELL_ONLY=t ./python311`

For running tests, make sure you are in the virtual environment, then run `./tools/test`
